# import Partial from './partial.graphql'

query Row {
  row {
    ...Partial
    code
  }
}

query Rows {
  rows {
    id
    name
    code
    description
    count
  }
}

mutation UpdateName($name: String!) {
  updateName(name: $name) {
    id
    name
    code
  }
}
